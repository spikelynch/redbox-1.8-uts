{
    "version": "1.1.0.1",
    "redbox.version.string": "${redbox.version}",
    "version.string": "${redbox.version}",
    "configured": true,
    "urlBase": "${server.url.base}",
    "email": "DO_NOT_CHANGE",
    "includeConfigDir" : "${fascinator.home}/config-include",
    "includeConfigExt" : ["json"]
}
